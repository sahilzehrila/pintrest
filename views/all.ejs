<% include ./partials/header.ejs %>

<div class="w-full min-h-screen bg-zinc-800 px-10 py-8 text-white">
    <div style="display: flex; flex-wrap: wrap;" class="cards flex flex-warp  gap-10">
        <% posts.forEach(function(e){ %>

     
     <div class="card w-32">
        <div class=" w-full h-60  bg-zinc-700 rounded-lg overflow-hidden">
            <a href="/details2">
            <img  class="w-full h-full object-cover" src="/images/uploads/<%= e.image  %>" alt="">
        </a>
        </div>
        <h5 class="mt-2 text-sm"><%= e.title  %></h5>
        <h6 class="text-xs opacity-500">@<%= e.user.username  %></h6>
    </div>
    <% }) %>
    </div>

</div>

<% include ./partials/footer.ejs %>
